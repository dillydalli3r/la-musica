# What the iOS bundle installs into its site-packages.
#
# Pinned to what server/requirements.txt pins, plus the pydantic chain the
# backend needs at import time. pystray (desktop tray icon) is dropped.
fastapi==0.141.1
# The pydantic chain is pinned here rather than left to pip, because the bundle
# has to BUILD pydantic-core: pinning it is what makes the wheel CI caches the
# same wheel the next build would have installed.
pydantic==2.13.5
pydantic-core==2.46.5
uvicorn==0.52.4
websockets==17.1
python-multipart==0.0.32
aiofiles==25.1.0
httpx==0.28.1
mutagen==1.48.1
Pillow==12.3.0
